---
import BaseLayout from '../layouts/BaseLayout.astro';
import site from '../content/site.json';

const pageTitle = "Thank You! | Ghareeb Fencing Solutions";
const pageDescription = "Thank you for contacting Ghareeb Fencing Solutions. We'll be in touch shortly.";
---

<BaseLayout title={pageTitle} description={pageDescription}>
  <style>
    .thank-you-page {
      min-height: 80vh;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 60px 20px;
      background: linear-gradient(135deg, var(--bg-light), var(--bg-card));
    }
    .thank-you-card {
      max-width: 600px;
      background: var(--bg-card);
      border-radius: var(--radius-lg);
      padding: 48px;
      text-align: center;
      box-shadow: var(--shadow-lg);
      border: 1px solid var(--border);
    }
    .success-icon {
      width: 80px;
      height: 80px;
      background: linear-gradient(135deg, #10b981, #059669);
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      margin: 0 auto 24px;
      animation: pop 0.5s ease;
    }
    @keyframes pop {
      0% { transform: scale(0); }
      50% { transform: scale(1.2); }
      100% { transform: scale(1); }
    }
    .success-icon svg {
      width: 40px;
      height: 40px;
      color: white;
    }
    .thank-you-card h1 {
      font-size: 2rem;
      margin-bottom: 16px;
      color: var(--text-dark);
    }
    .thank-you-card p {
      color: var(--text-muted);
      font-size: 1.1rem;
      margin-bottom: 24px;
      line-height: 1.7;
    }
    .next-steps {
      background: var(--bg-light);
      border-radius: var(--radius);
      padding: 24px;
      margin: 24px 0;
      text-align: left;
    }
    .next-steps h3 {
      margin-bottom: 16px;
      font-size: 1rem;
      color: var(--text-dark);
    }
    .next-steps ul {
      list-style: none;
      padding: 0;
      margin: 0;
    }
    .next-steps li {
      padding: 8px 0;
      display: flex;
      align-items: flex-start;
      gap: 12px;
      color: var(--text-body);
    }
    .next-steps li::before {
      content: '✓';
      color: #10b981;
      font-weight: bold;
      flex-shrink: 0;
    }
    .contact-info {
      display: flex;
      gap: 24px;
      justify-content: center;
      margin-top: 32px;
      flex-wrap: wrap;
    }
    .contact-item {
      display: flex;
      align-items: center;
      gap: 8px;
      color: var(--text-body);
    }
    .contact-item a {
      color: var(--primary);
      text-decoration: none;
      font-weight: 600;
    }
    .contact-item a:hover {
      color: var(--accent);
    }
    .cta-buttons {
      display: flex;
      gap: 16px;
      justify-content: center;
      margin-top: 32px;
      flex-wrap: wrap;
    }
    .cta-buttons .btn {
      padding: 14px 28px;
      border-radius: var(--radius-full);
      font-weight: 600;
      text-decoration: none;
      transition: all 0.2s;
    }
    .btn-primary {
      background: var(--accent);
      color: var(--primary-dark);
    }
    .btn-secondary {
      background: var(--primary);
      color: white;
    }
  </style>

  <section class="thank-you-page">
    <div class="thank-you-card">
      <div class="success-icon">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round">
          <polyline points="20 6 9 17 4 12"></polyline>
        </svg>
      </div>
      
      <h1>Thank You! 🎉</h1>
      <p>Your message has been received. We'll be in touch within 24 hours (usually much faster!).</p>
      
      <div class="next-steps">
        <h3>What happens next?</h3>
        <ul>
          <li>We'll review your project details</li>
          <li>A team member will call to schedule your free on-site consultation</li>
          <li>We'll provide a detailed written quote with no obligation</li>
          <li>If you proceed, we'll handle permits and schedule installation</li>
        </ul>
      </div>
      
      <p style="font-size: 0.95rem;">Need to reach us sooner? Give us a call:</p>
      
      <div class="contact-info">
        <div class="contact-item">
          📞 <a href="tel:4199028257">(419) 902-8257</a>
        </div>
        <div class="contact-item">
          ✉️ <a href="mailto:nick@ghareebfencing.com">nick@ghareebfencing.com</a>
        </div>
      </div>
      
      <div class="cta-buttons">
        <a href="/" class="btn btn-primary">← Back to Home</a>
        <a href="/fences/" class="btn btn-secondary">Browse Fence Styles</a>
      </div>
    </div>
  </section>
</BaseLayout>
